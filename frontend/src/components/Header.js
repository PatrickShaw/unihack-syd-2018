import React, { Component } from 'react';
import withStyles from '@material-ui/core/styles/withStyles';
export const Header = withStyles({
  
})(class extends Component { 
  render() {
    return (
      <header>
        {
          // TODO: Add the logo here or something?
        }
      </header>
    );
  }
})